import { Login, SignUp } from "../../../pages"

const userVerificationRoutes = [
    {
        path: "login",
        element: <Login />
    },
    {
        path: "signup",
        element: <SignUp />
    }
]

export default userVerificationRoutes